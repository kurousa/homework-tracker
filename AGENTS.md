# Project Guidelines (SSoT)

このファイルは「きょうのしゅくだい (Homework Tracker)」プロジェクトにおける固有のガイドラインを記述する場所です。
開発者が一貫した方針で実装を行えるよう、技術的決定事項、アーキテクチャ方針、コーディングルール、主要コマンドをまとめています。

---

## 1. プロジェクト概要

「きょうのしゅくだい (Homework Tracker)」は、親子で楽しく宿題や今日の目標を管理・追跡できるWebアプリケーションです。
ワンタップで切り替え可能な「保護者モード」と「子どもモード」のダブル・テーマUIを備え、保護者が設定した宿題をお子様がクリアしていくことで達成感を促す設計となっています。

## 2. 技術スタック (Technology Stack)

- **フロントエンド**: Vue 3 (Composition API / `<script setup>` 構文)
- **ビルド・バンドル**: Vite
- **状態管理 (State Store)**: Pinia
- **スタイリング**: TailwindCSS + PostCSS + Font Awesome 6 (アイコン用)
- **演出効果**: canvas-confetti (宿題オールクリア時の紙吹雪演出)
- **テスト**: Vitest + @vue/test-utils + jsdom

## 3. ディレクトリ構成と設計方針

```text
src/
├── assets/         # 静的アセット（画像、ベースのCSSスタイルなど）
├── components/     # UIコンポーネント
│   ├── common/     # プロジェクト全体で使い回す共通コンポーネント（Header, Toast等）
│   ├── dashboards/ # 各モード専用のダッシュボード（ParentDashboard, ChildDashboard）
│   └── modals/     # 設定や入力用の各種モーダル（PinModal, ChildSelectModal等）
├── stores/         # Piniaによる状態管理（Auth, Child, Homework, Toastなど機能ごとに分離）
├── utils/          # 汎用ヘルパー、演出制御ロジック（confetti.js等）
├── App.vue         # メインコンポーネント（テーマの切り替えや全体レイアウトの制御）
└── main.js         # エントリーポイント
```

## 4. 開発・実装上の重要ルール

### 4.1. 状態管理のルール
- **コンポーネント内への状態の閉じ込め禁止**:
  宿題リスト、登録された子ども情報、認証状態などのアプリケーション全体の重要なデータは、コンポーネント内に閉じ込めず、必ず対応する Pinia ストア（`useHomeworkStore`, `useChildStore`, `useAuthStore`）に定義してください。
- **データ永続化**:
  ユーザーがデータを失わないよう、ストア内でのデータの更新時には `localStorage` への同期処理を適切に組み込んでください。

### 4.2. UI/UX・スタイリングのルール
- **ダブル・テーマの厳守**:
  - **保護者モード (Parent Mode)**:
    - 落ち着いたプロフェッショナルなデザイン、フォントに `Inter` を使用します。
    - 宿題の追加・編集・削除など、すべての管理機能にアクセス可能です。
  - **子どもモード (Child Mode)**:
    - カラフルで親しみやすい丸ゴシック風のデザイン、フォントに `Zen Maru Gothic` を使用します。
    - テキストはひらがな・カタカナを多用し、直感的なアイコンと大きなボタンで操作できるようにします。
- **レスポンシブデザイン**:
  スマートフォンやタブレットなどのモバイル端末で親子が一緒に使用することを前提とし、TailwindCSS を活用した完全なモバイルフレンドリーなレスポンシブレイアウトを維持してください。

### 4.3. セキュリティと認証 (ペアレンタルコントロール)
- **保護者モードへのアクセス制限**:
  子どもが誤って保護者機能に入りデータを削除・改ざんするのを防ぐため、保護者モードへの切り替え時には必ず 4桁のPINコード入力（`useAuthStore` 経由の認証）を要求してください。
- PINコードが未設定の初回起動時には、PINコード設定モーダルが適切に表示されるように制御してください。

### 4.4. テスト方針
- ビジネスロジックを担う Pinia ストアや、振る舞いを持つ重要なコンポーネントに対しては、Vitest と `@vue/test-utils` を用いたユニットテストを整備してください。
- テストファイルは、テスト対象ファイルと同一または近接するディレクトリ、あるいは規約に沿った形で配置し、カバレッジを維持します。

## 5. 主要な開発コマンド

- **依存関係のインストール**:
  ```bash
  npm install
  ```
- **ローカル開発サーバー起動**:
  ```bash
  npm run dev
  ```
- **本番用ビルド**:
  ```bash
  npm run build
  ```
- **テストの実行**:
  ```bash
  npm run test
  ```
- **ビルド成果物のプレビュー**:
  ```bash
  npm run preview
  ```

---

## 参照先
- **行動規範:** `.agent/rules/` を参照してください。
- **ワークフロー:** `.agent/workflows/` を参照してください。
- **スキル:** `.agent/skills/` を参照してください。